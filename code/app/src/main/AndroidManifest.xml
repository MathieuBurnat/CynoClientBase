<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ch.leytto.cynoclient">

    <application
        android:name=".CynoClientApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CynoClient">
        <activity
            android:name=".DogDetailsActivity"
            android:label="Détails d'un chien" />
        <activity
            android:name=".ClientInfoActivity"
            android:label="Détails d'un client" />
        <activity
            android:name=".CreateClientActivity"
            android:label="Création d'un client" />
        <activity
            android:name=".ReportDetailsActivity"
            android:label="Détails d'une consultation" />
        <activity
            android:name=".ServiceDetailsActivity"
            android:label="Détails d'une consultation" />
        <activity
            android:name=".LocalityDetailsActivity"
            android:label="Détails d'une consultation" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.CynoClient.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>